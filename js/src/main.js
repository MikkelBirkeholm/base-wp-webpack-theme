
let Main = {
  init: async function () {

    // initialize demo javascript component - async/await invokes some 
    //  level of babel transformation

    // const navigation = new Navigation();
    // await navigation.init();
    console.log('Side lavet af mikkelbirkeholm.dk');

  }
};

Main.init();
